{
  "devDependencies": {
    "jasmine-core": "^2.4.1",
    "jquery": "^2.1.4",
    "karma": "^0.13.15",
    "karma-jasmine": "^0.3.6",
    "karma-jasmine-jquery": "^0.1.1",
    "karma-phantomjs-launcher": "^0.2.1",
    "phantomjs": "^1.9.19"
  },
  "scripts": {
    "test": "karma start >/dev/null & sleep 5 && karma run && ps -ef | grep karma | grep -v grep | awk '{print $2}' | xargs kill -9"
  }
}
